{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "rm -rf ./build && tsc",
    "start": "node --loader ts-node/esm ./app.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.1.0",
    "tslib": "^2.0.0",
    "typescript": "^4.0.0"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongoose": "^5.13.3",
    "sirv-cli": "^1.0.0"
  }
}
